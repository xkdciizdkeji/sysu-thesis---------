{
    "build": {
        "dockerfile": "Dockerfile",
        "options": [
            "--network=host"
        ]
    },
    "postCreateCommand": "node -v && npm -v && npx -v",
    "customizations": {
        "vscode": {
            "extensions": [
                "ms-ceintl.vscode-language-pack-zh-hans",
                "james-yu.latex-workshop",
                "nickfode.latex-formatter"
            ]
        }
    }
}